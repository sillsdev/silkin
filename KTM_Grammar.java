/**		This class exists only to document the grammar used to parse the {@link KinTermMatrix}        files (*.ktm) stored in the Library.  Each .ktm file records the matrix for one        context (culture or language).  This grammar is coded into the class {@link KTMParser}.<p>		KTM -> Matrix, IndexRef, IndexAddr.<p>Matrix -> <matrix>, Rows, </matrix>.<p>Rows -> <row>, integer, CellPairs, </row>, Rows.     |  \empty.<p>CellPairs -> integer, Cell, CellPairs.          |  \empty.<p>Cell -> <tag>, Value, TagValPairs.<p>TagValPairs -> <tag>, Value, TagValPairs.            |  \empty.<p>Value -> integer.      |  List.      |  symbol.      |  string.            <p>      List -> leftParen, Item, OtherItems, rightParen. <p>Item -> integer.     |  symbol.     |  List.     |  string.<p>OtherItems -> comma, Item, OtherItems.           |  \empty.<p>IndexRef -> <indexRef>, IndexRows, </indexRef>.<p>IndexRows -> <row>, symbol, IndexPairs, </row>.          |  \empty<p>IndexPairs -> integer, IntList, IndexPairs.           |  \empty.<p>IntList -> leftParen, Integers, rightParen.<p>Integers -> integer, OtherIntegers.<p> OtherIntegers -> comma, integer, OtherIntegers. |  \empty. <p> IndexAddr -> <indexAddr>, IndexRows, </indexAddr>.*/public abstract class KTM_Grammar  {  }      